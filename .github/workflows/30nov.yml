
name: 30 nov
on:
  push:
    branches:
      - main
jobs:
  Hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo the message
        run: |
          echo "Hola Juan"

      - name: Dime la fecha
        run: date
      - run: ls -l

